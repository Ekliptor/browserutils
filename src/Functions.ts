
// types for functions.js
// TODO port our helpers to typescript to get type information
//export type AppF = any;
export type AppFunc = any;

/*
export class AppFunc {
    public static readonly cfg = {
    }

    protected fileDownloadCount = 0;

    constructor() {
    }

    public loadResource(filename, callback, checkFn, inOrder) {
    }
}
*/